import 'package:flutter/material.dart';

class SharedWorkouts extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Shared Workouts'),
      ),
      body: Center(
        child: Text('Shared Workouts Page'),
      ),
    );
  }
}